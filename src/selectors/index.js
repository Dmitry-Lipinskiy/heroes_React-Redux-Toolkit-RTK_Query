export const filtersSelector = (state) => state.filters.filters;

export const filtersLoadingStatusSelector = (state) => state.filters.filtersLoadingStatus;

export const activeFilterSelector = (state) => state.filters.activeFilter;
